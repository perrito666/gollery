# Prompt 01 — Backend repo skeleton

Create the initial Go backend skeleton under `backend/`.

Include:
- `go.mod`
- `cmd/galleryd/main.go`
- empty internal package directories from the design doc
- placeholder files so the module builds
- backend README
- Makefile targets for build/test

Do not implement business logic yet.
Return full file tree and contents.
