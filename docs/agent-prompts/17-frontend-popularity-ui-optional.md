# Prompt 17 — Frontend optional popularity UI

Implement optional popularity presentation.

Requirements:
- feature-flagged
- consume backend popularity endpoints if available
- never assume analytics are enabled
- do not expose restricted object popularity improperly

Examples:
- popular assets in album
- admin-only popularity panel
