# Prompt 02 — Backend config and domain

Implement:
- global app config structs
- `album.json` config structs
- domain structs for Album, Asset, Snapshot, Principal, DiscussionBinding
- validation and merge rules
- tests

Keep `album.json` declarative only.
Do not implement scanning yet.
