# Prompt 44 — Frontend login loading state

Add visual loading feedback to the login form.

Implement:
- disable the submit button while the login request is in flight
- show a loading indicator or text on the button (e.g., "Logging in…")
- re-enable the button on success or failure
- prevent double-submit

Do not change the login success/error behavior.
