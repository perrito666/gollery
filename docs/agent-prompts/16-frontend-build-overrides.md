# Prompt 16 — Frontend build and override system

Implement:
- build resolution for `ui-default` + `site` overrides
- `make build`
- `make print-effective-theme`
- site config loading
- CSS override support

Do not redesign core functionality.
