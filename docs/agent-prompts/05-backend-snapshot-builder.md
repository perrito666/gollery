# Prompt 05 — Backend snapshot builder

Implement the snapshot builder.

Requirements:
- combine scanner output + sidecar state
- attach stable IDs
- build album hierarchy
- attach asset metadata placeholders
- tests

Do not implement HTTP handlers yet.
